@model ApprenticeshipManagement.ViewModels.AdminLoginModel
@{
    ViewData["Title"] = "Admin Login";
}

<div class="login-wrap">
    <div class="form-box form-box-red">
        <div class="form-head text-center mb-4">
            <img src="/images/drdo-logo.png" alt="DRDO Logo" class="logo-big">
            <span class="badge bg-danger mb-2">Administrator</span>
            <h2 class="form-title">Welcome Back!</h2>
            <p class="text-muted">Login to your admin account</p>
        </div>

        @if (TempData["Success"] != null)
        {
            <div class="alert alert-success">@TempData["Success"]</div>
        }

        <form asp-action="AdminLogin" method="post" novalidate>
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="mb-3">
                <label asp-for="Email" class="form-label">Username or Email</label>
                <input asp-for="Email" class="form-control" placeholder="admin@example.com" autofocus />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Password" class="form-label">Password</label>
                <input asp-for="Password" class="form-control" type="password" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="mb-4 form-check">
                <input asp-for="RememberMe" class="form-check-input" />
                <label asp-for="RememberMe" class="form-check-label">Remember me</label>
            </div>

            <button type="submit" class="btn btn-primary w-100">
                <i class="bi bi-box-arrow-in-right"></i> Login
            </button>
        </form>

        <div class="or-line mt-4">or</div>

        <div class="d-grid gap-2">
            <a asp-action="ApprenticeLogin" class="btn btn-outline-primary">
                <i class="bi bi-person-badge"></i> Login as Apprentice
            </a>
        </div>

        <p class="text-center mt-4 mb-0">
            Don't have an account? 
            <a asp-action="AdminRegister" class="text-primary fw-bold">Register here</a>
        </p>
        <p class="text-center mt-2 mb-0 small">
            <a asp-controller="Home" asp-action="Index" class="text-muted">Back to Home</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
